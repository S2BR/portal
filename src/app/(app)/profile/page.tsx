/**
 * `/profile` is the "settings open" URL — the settings dialog (mounted app-wide in the root layout,
 * see {@link SettingsDialogProvider}) shows itself whenever the app is on this path. This page is
 * just the (dimmed) backdrop behind that dialog; closing it navigates away.
 */
export default function ProfilePage() {
  return null;
}
